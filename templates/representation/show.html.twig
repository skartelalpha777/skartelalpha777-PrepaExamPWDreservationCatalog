{% extends 'base.html.twig' %}

{% block title %}Representation{% endblock %}

{% block body %}
    <h1>Representation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ representation.id }}</td>
            </tr>
            <tr>
                <th>Schedule</th>
                <td>{{ representation.schedule ? representation.schedule|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_representation_index') }}">back to list</a>

    <a href="{{ path('app_representation_edit', {'id': representation.id}) }}">edit</a>

    {{ include('representation/_delete_form.html.twig') }}
{% endblock %}
