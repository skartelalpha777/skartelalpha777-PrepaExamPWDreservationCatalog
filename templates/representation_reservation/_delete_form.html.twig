<form method="post" action="{{ path('app_representation_reservation_delete', {'id': representation_reservation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ representation_reservation.id) }}">
    <button class="btn">Delete</button>
</form>
