{% extends 'base.html.twig' %}

{% block title %}RepresentationReservation{% endblock %}

{% block body %}
    <h1>RepresentationReservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ representation_reservation.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ representation_reservation.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_representation_reservation_index') }}">back to list</a>

    <a href="{{ path('app_representation_reservation_edit', {'id': representation_reservation.id}) }}">edit</a>

    {{ include('representation_reservation/_delete_form.html.twig') }}
{% endblock %}
